# Dotfiles

A collection of my dotfiles.
They present here due to version control.

## Installation
The install script is a self-contained bash script, no pre-tools required.

**Install dotfiles**
```sh
./install.sh -t neovim -v
```
To see more options
```sh
./install.sh -h
```

## Testing
No test scripts provided for now
